##################################################
# Travis CI
##################################################

language: java

git:
  depth: 50

jdk: oraclejdk8

addons:
  apt:
    packages:
      - oracle-java8-installer

os: linux

matrix:
  fast_finish: true
  include:
    - env: BUILD_TYPE=DEBUG
    - env: BUILD_TYPE=RELEASE
    - env: BUILD_TYPE=DEBUG APPLY_LAST_GOOD_SETTINGS=true
    - env: BUILD_TYPE=RELEASE APPLY_LAST_GOOD_SETTINGS=true

before_install:
  - chmod +x ./gradlew

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

branches:
  except:
    - gh-pages
