##################################################
# Travis CI
##################################################

language: java

git:
  depth: 50

jdk: oraclejdk8

addons:
  apt:
    packages:
      - oracle-java8-installer

os: linux

matrix:
  fast_finish: true
  include:
    - env: PbuildType=DEBUG
    - env: PbuildType=RELEASE
    - env: PbuildType=DEBUG PapplyLastGoodSettings=true
    - env: PbuildType=RELEASE PapplyLastGoodSettings=true

before_install:
  - chmod +x ./gradlew

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

branches:
  except:
    - gh-pages
